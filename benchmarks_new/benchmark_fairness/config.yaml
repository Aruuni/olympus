kind: olympus-eval
version: 1
defaults:
  n_parallel: 20
  repetitions: 4
  seed: 0
  logging: standard
  output_root: data
matrix:
  checkpoints: [test-protocol, orca-olympus]
  scenarios: [scenario.yaml]
  environments: [raynet]
