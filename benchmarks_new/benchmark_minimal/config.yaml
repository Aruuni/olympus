kind: olympus-eval
version: 1
defaults:
  n_parallel: 4
  repetitions: 4
  seed: 0
  logging: standard
  output_root: data
matrix:
  checkpoints: [test-protocol]
  scenarios: [scenario.yaml]
  environments: [mininet, raynet]
