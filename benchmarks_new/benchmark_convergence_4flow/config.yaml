kind: olympus-eval
version: 1
defaults:
  n_parallel: 10
  repetitions: 3
  seed: 0
  logging: standard
  output_root: data
matrix:
  checkpoints: [test-protocol]
  scenarios: [scenario.yaml]
  environments: [mininet, raynet]
