kind: olympus-eval
version: 1
defaults:
  n_parallel: 10
  repetitions: 5
  seed: 0
  logging: standard
  output_root: data
matrix:
  checkpoints: [Orca]
  scenarios: [scenario.yaml]
  environments: [raynet]
