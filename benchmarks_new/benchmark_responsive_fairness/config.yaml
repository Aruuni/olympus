kind: olympus-eval
version: 1
defaults:
  n_parallel: 30
  repetitions: 50
  seed: 0
  logging: standard
  output_root: data
matrix:
  checkpoints: [tempest_static]
  scenarios: [scenario.yaml]
  environments: [mininet, raynet]
