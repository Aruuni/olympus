config:
  defaults:
    n_parallel: 5
    repetitions: 2
    output_root: data_debug

scenario:
  name: paper_intra_rtt_fairness_debug
  sweep:
    delays: [20, 40, 60, 80, 100]
    duration: 20
    score_window_s: 10
    flow_schedule:
      arrival: {start_delays: [0, 5]}
