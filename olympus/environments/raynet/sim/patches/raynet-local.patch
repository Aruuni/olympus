diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9e737b1..5c86b9f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -2,7 +2,7 @@ cmake_policy(SET CMP0048 NEW)
 cmake_minimum_required(VERSION 3.5...3.18)
 project(rlomnetbind VERSION 1.0.0 LANGUAGES CXX)
 
-set(CMAKE_CXX_STANDARD 20)
+set(CMAKE_CXX_STANDARD 17)  # match OMNeT++ (built -std=c++17); GCC 11 libstdc++ lacks C++20 <format>
 set(CMAKE_DEBUG_POSTFIX _dbg)
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
 set(CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR}/cmake)
@@ -33,13 +33,18 @@ set(TCPGOODPUT_PATH $ENV{OMNET_PATH}/samples/tcpGoodputApplications/src) # tcpGo
 list(APPEND LIB_PATHS ${TCPGOODPUT_PATH})
 list(APPEND LIB_NAMES tcpGoodputApplications)
 
-set(LEOSATELLITES_PATH $ENV{OMNET_PATH}/samples/leosatellites/src) # leosatellites, not strictly necessary but contains features that ORBTCPEXPERIMENTS needs
-list(APPEND LIB_PATHS ${LEOSATELLITES_PATH})
-list(APPEND LIB_NAMES leosatellites)
-
-set(ORBTCPEXPERIMENTS_PATH $ENV{OMNET_PATH}/samples/orbtcpExperiments/src) # orbtcpExperiments, Aiden's experiment suite with some custom components like BandwidthRecorderDropTailQueue
-list(APPEND LIB_PATHS ${ORBTCPEXPERIMENTS_PATH})
-list(APPEND LIB_NAMES orbtcpExperiments)
+# NOTE: leosatellites / orbtcpExperiments are the satellite + orbtcp experiment
+# extensions, not required for CC training (Orca/Astraea/CleanSlate/DeepCC).
+# Trimmed from the link list for the Olympus build: their apt deps (pkg-config,
+# libigraph-dev) are not installed here. tcpGoodputApplications stays linked
+# because base_environment.ini uses TcpGoodputSessionApp.
+# set(LEOSATELLITES_PATH $ENV{OMNET_PATH}/samples/leosatellites/src)
+# list(APPEND LIB_PATHS ${LEOSATELLITES_PATH})
+# list(APPEND LIB_NAMES leosatellites)
+#
+# set(ORBTCPEXPERIMENTS_PATH $ENV{OMNET_PATH}/samples/orbtcpExperiments/src)
+# list(APPEND LIB_PATHS ${ORBTCPEXPERIMENTS_PATH})
+# list(APPEND LIB_NAMES orbtcpExperiments)
 
 set(TCPPACED_PATH $ENV{OMNET_PATH}/samples/tcpPaced/src) # tcpPaced, has pacing mechanisms for CC schemes, also has an auto network configurator that needs to be linked early
 list(APPEND LIB_PATHS ${TCPPACED_PATH})
diff --git a/_environments/base_environment.ini b/_environments/base_environment.ini
index 40267b2..10d4f8b 100644
--- a/_environments/base_environment.ini
+++ b/_environments/base_environment.ini
@@ -1,5 +1,5 @@
 [General]
-ned-path = !RAYNET_PATH!/_topologies:!INET!/examples:!INET!/showcases:!INET!/src:!INET!/tests/validation:!INET!/tests/networks:!INET!/tutorials:!OMNETPP!/samples/tcpPaced/src:!OMNETPP!/samples/tcpGoodputApplications/src:!OMNETPP!/samples/cubic/src:!OMNETPP!/samples/orbtcpExperiments/src:!RAYNET_PATH!/simlibs/RLComponents/src:!RAYNET_PATH!/simlibs/Orca/src:!RAYNET_PATH!/simlibs/Astraea/src:!RAYNET_PATH!/simlibs/DeepCC/src:!RAYNET_PATH!/simlibs/CleanSlate/src:
+ned-path = !RAYNET_PATH!/_topologies:!INET!/examples:!INET!/showcases:!INET!/src:!INET!/tests/validation:!INET!/tests/networks:!INET!/tutorials:!OMNETPP!/samples/tcpPaced/src:!OMNETPP!/samples/tcpGoodputApplications/src:!OMNETPP!/samples/cubic/src:!RAYNET_PATH!/simlibs/RLComponents/src:!RAYNET_PATH!/simlibs/Orca/src:!RAYNET_PATH!/simlibs/Astraea/src:!RAYNET_PATH!/simlibs/DeepCC/src:!RAYNET_PATH!/simlibs/CleanSlate/src:
 
 **.scenarioManager.script = xmldoc("!SCENARIO!")
 
@@ -63,7 +63,11 @@ network = topologies.Dumbbell
 **.server[*].tcp.tcpAlgorithmClass = "!CC_ALGO!"
 
 # Queue settings
-**.ppp[*].queue.typename = "orbtcpexperiments.queueing.queue.BandwidthRecorderDropTailQueue"
+# Olympus-local: BandwidthRecorderDropTailQueue lives in orbtcpExperiments,
+# whose dependency chain (os3/leosatellites) needs apt packages not installed
+# here. It only adds bandwidth output vectors for plotting; behavior matches
+# INET's DropTailQueue, so use that instead.
+**.ppp[*].queue.typename = "DropTailQueue"
 
 # RL component settings
 **.printDebugMessages = true
